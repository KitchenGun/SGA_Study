#include <iostream>
#include <vector>
#include <queue>

using namespace std;

void dfs(vector<int>&dgree,int start,int end)
{
	que
}

int main()
{
	int n,m;
	cin>>n>>m;
	vector<int> dgree(41,-1);

	dfs(dgree,n,m);
	cout<<dgree[m]
}